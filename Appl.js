const webForm = (function() {
  //get the possible variable or const or let  i will like to be using
          let form;
          let input;

          function init(){
            cacheDom();
            bindEvents();
          }
  
        })();
        webForm.init();
      //  render: function () {
    //        var check = {
          //      learn: this.learn,
        //    };
      //      this.$el-form(Mustache.render(this.input, check));
    //    },

     //   checkInputs: 



     checkInputs:  function() {
            alert('a,em');
          
            if (input1 == " ") {
                alert("hey go");
                return false;
            }
           },
     };
   
})()
learn.init();

$input.forEach(($inputEl) => validateForm($inputEl))
input.classList.add('hey')
input.classLidt.remove('hey')